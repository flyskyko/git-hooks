#!/bin/bash

file="$HOME/.gitauthor"
export NEW_NAME=$(head -1 $file)
export NEW_EMAIL=$(tail -1 $file)
number=$(wc -l | awk {'print $1'})

git filter-branch -f --env-filter '
	export GIT_COMMITTER_NAME="$NEW_NAME"
	export GIT_COMMITTER_EMAIL="$NEW_EMAIL"
' "HEAD~$number..HEAD"
